package com.example.captureit.services

/**
 * Enum class representing the possible actions for the Location Service.
 * Used to control the start and stop operations of the service.
 */
enum class ServiceAction {
    /**
     * Action to start the service.
     */
    START_SERVICE,

    /**
     * Action to stop the service.
     */
    STOP_SERVICE
}
